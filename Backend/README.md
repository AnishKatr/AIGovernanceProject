# Flask Backend Setup

## Install dependencies
```zsh
cd Backend
pip install -r requirements.txt
```

## Run the server
```zsh
python app.py
```

## Test the API
Visit: http://localhost:5000/api/hello

You should see: `{ "message": "Hello from Flask backend!" }`
